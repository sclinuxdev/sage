name: Torture Lab

on:
  workflow_dispatch:
    inputs:
      seed:
        description: Reproducible random seed
        required: true
        default: "1517166630"
      operations:
        description: Random state-machine operations
        required: true
        default: "1000"
      packages:
        description: Database benchmark package count
        required: true
        default: "10000"
  schedule:
    - cron: "17 3 * * *"

permissions:
  contents: read

jobs:
  random-state-machine:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Run long model sequence
        env:
          TORTURE_SEED: ${{ inputs.seed || '1517166630' }}
          TORTURE_OPERATIONS: ${{ inputs.operations || '1000' }}
        run: >-
          cargo run -p sage-tests --bin sage-torture -- random
          --seed "$TORTURE_SEED" --operations "$TORTURE_OPERATIONS"

  benchmark:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Record database and package-path benchmarks
        env:
          TORTURE_PACKAGES: ${{ inputs.packages || '10000' }}
        run: >-
          cargo run --release -p sage-tests --bin sage-torture -- bench
          --packages "$TORTURE_PACKAGES"
